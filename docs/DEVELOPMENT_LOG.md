# Журнал разработки ЦУИМ

> Официальный человекочитаемый журнал создания городского маркетплейса ЦУИМ.
>
> Репозиторий: `nyaganmaksimov-sys/cuim-marketplace`  
> Начало истории Git: **06.11.2025**  
> Первый коммит: `cffe249f4e0e58a371cbf79a5a140edfc0da1937` — `Initial commit`  
> История восстановлена от первого коммита и дальше ведётся постоянно.

## Как ведётся журнал

Этот файл — обзорный реестр развития продукта. Полным неизменяемым техническим источником истины остаётся Git. Здесь содержательные коммиты объединяются по пользовательскому результату: функции, интерфейс, БД, RPC, RLS, Storage, Edge Functions, безопасность, миграции и тесты.

Правила:

- после завершённого этапа добавляется запись;
- указываются дата, подсистема, результат, техническое решение, проверка и SHA;
- изменения Supabase фиксируются вместе с относящимся к ним продуктовым этапом;
- секреты, платёжные ключи, токены и персональные данные не записываются;
- мелкие коммиты могут объединяться в один этап;
- ежедневный аудит проверяет пропущенные изменения.

**Последняя учтённая продуктовая точка:** `e3f1aa5884103252f4f13069632b7ca109212ec3`

---

# 1. Начало проекта и первый прототип

## 06.11.2025 — создание репозитория

**Статус:** DONE  
**Подсистема:** repository / frontend

- Создан репозиторий будущего маркетплейса ЦУИМ.
- Первый коммит: `cffe249` (`Initial commit`).
- Начались многочисленные итерации `index.html`, CSS, логотипов и изображений категорий.
- Сформировалась первоначальная структура главной страницы и визуальная концепция.
- Ранние механические изменения полностью сохранены в Git (`Update index.html`, `Update style.css`, `Add files via upload`, `Delete ...`).

## 06.11.2025 — проектная документация

- Создан `ROADMAP.md` — `3612330...`.
- Создан `CONTRIBUTING.md` — `1d52db7...`.
- Развитие продукта стало отделяться от единичных правок страницы.

---

# 2. Формирование городского маркетплейса

## Главная и базовая витрина

- Главная стала городской витриной с рекламным слайдером, поиском, разделами и карточками предложений.
- Сформированы направления **Товары, Услуги, Работа, Еда, Авто, Объявления, Афиша**.
- Созданы профили компаний/продавцов и переходы к предложениям участника.
- Добавлены корзина, личный кабинет и базовая навигация.
- Каталог постепенно переведён со статических демо-данных на Supabase.

## Supabase как backend

- Supabase стал основной БД/backend платформы.
- Созданы сущности продавцов, предложений, пользователей, заказов и вспомогательных процессов.
- Публичная выдача отделена от управляющих операций.
- Началась системная работа с RLS и RPC.
- Пользовательские файлы и изображения вынесены в Supabase Storage.

---

# 3. Таксономия и единый каталог

## Городские разделы

- Созданы `marketplace_city_sections` и `marketplace_city_subcategories`.
- Поддерживаются `all / goods / services / jobs / food / auto / ads / events`.
- Сформировано более 60 подкатегорий.
- Иконки разделов переведены в БД.
- Создан административный редактор `/admin/categories.html`.

## Типизированные предложения

- В `partner_products` добавлены `marketplace_section_key`, `marketplace_subcategory_key`, тип предложения и `marketplace_details`.
- Созданы публичные RPC каталога и карточки предложения.
- Введены маршруты `/catalog/<section>/<subcategory>/`.
- Главная и меню переведены на реальную таксономию.

---

# 4. Доска объявлений

## Специализированная вертикаль

- `/catalog/ads/` получил отдельный интерфейс, категории, desktop-фильтры и мобильную панель фильтров.

## Жизненный цикл объявления

Созданы:

- `/ad/?id=...` — карточка;
- `/post-ad.html` — создание;
- `/my-ads.html` — управление;
- `/edit-ad.html?id=...` — редактирование;
- `/admin/classifieds.html` — модерация.

Реализованы до 10 фото, черновики, модерация, статусы Active/Pending/Sold/Removed/Draft/Rejected, просмотры, жалобы, продление, повторная модерация после существенных правок и срок жизни активного объявления 30 дней.

## Сообщения и уведомления

- Созданы `/messages.html` и мини-чат в объявлении.
- Подключён Realtime.
- Создана `marketplace_notifications` и `/notifications.html`.
- Реализованы уведомления о модерации, сообщениях и сроке публикации.
- Cron предупреждает перед окончанием и завершает просроченные объявления.

---

# 5. Рекламная система

## Платное продвижение

- Созданы верхний баннер, продвигаемое объявление и встроенная/native реклама.
- Созданы `/advertise.html` и `/admin/advertising.html`.
- Публично публикуются только кампании **PAID + ACTIVE**.

## YooKassa

- Созданы Edge Functions `create-ad-payment` и `yookassa-ad-webhook`.
- Технические платёжные идентификаторы отделены от публичной выдачи.
- Ограничены допустимые рекламные URL.

## Маркировка рекламы РФ

- Добавлен обязательный ERID.
- Поддерживается ОРД/регистрационная информация.
- Активация без ERID блокируется.
- Публичная реклама показывает признак рекламы, рекламодателя и `erid`.
- Приватные регистрационные сведения отделены.

---

# 6. Beauty — «Красота и запись»

## Вертикаль красоты

Поддерживаются `NAILS`, `HAIR`, `BROWS`, `LASHES`, `MAKEUP`, `COSMETOLOGY`, `MASSAGE`, `BARBER`, `DEPILATION`, `OTHER`.

Созданы `/beauty-register.html`, `/beauty-workspace.html`, `/beauty-master.html`, `/beauty-salon.html`, `/beauty-bookings.html`, специализированный `/catalog/services/beauty/`.

Созданы профили, услуги, окна, недельные графики, перерывы/выходные, записи, салоны, портфолио и отзывы.

## Онлайн-запись

- Свободные интервалы рассчитываются по графику и длительности услуги.
- Учитываются перерывы и закрытые периоды.
- Двойная запись блокируется транзакционно.
- Прошедшее время не выдаётся как свободное.
- Запись/отмена/перенос клиента требуют авторизации.

## Салон, портфолио и отзывы

- Владелец салона может вести несколько мастеров.
- RLS учитывает владельца мастера и салона.
- Портфолио хранится в защищённом Storage.
- Отзыв разрешён после завершённого визита; рейтинг 1–5, поддержан ответ мастера.

## Beauty-каталог — редизайн

**Коммит:** `2576c9587649c5b9d71f45f299ec81cb52155cd7`.

- Профессиональный светлый интерфейс без emoji.
- SVG-иконки, specialty-карточки, фильтры, сортировка.
- Цена «от», рейтинг, опыт, салон, услуги и отзывы.
- Мобильная версия категорий/фильтров.

## Beauty Pro — календарь и CRM

Созданы `/beauty-calendar.html` и `/beauty-clients.html`.

Реализованы День/Неделя, 30-минутная сетка, ручная запись без аккаунта клиента, перенос/drag&drop, статусы визита, выручка, история клиента, приватные CRM-заметки, теги и повторная запись.

Серверно проверено, что чужой пользователь не получает приватные CRM-заметки.

---

# 7. Мобильная версия и главный слайдер

## Мобильная главная

**Ключевой коммит:** `2d38f2d7d234b6bc39c6c2a5c2d9beb0df299782`.

- `homepage-mobile.css`, компактная шапка, сообщения/корзина/кабинет.
- Разделы переведены в горизонтальную прокрутку.
- Товарная сетка адаптирована.
- Добавлена нижняя мобильная навигация.
- Beauty выведен отдельным блоком.

## Рекламные баннеры

- Подготовлены desktop/mobile креативы Beauty, Объявлений и других возможностей.
- Рабочие форматы: desktop `1920×600`, mobile `1536×1024`.

## Исправление обрезки

**Коммит:** `250f1ed25ebc010831b07d779f2aa336f2408cf6`.

- Мобильный слайдер переведён с 16:9 на 3:2.
- `object-fit: cover` заменён на `contain` для мобильных баннеров.
- Устранена обрезка креативов 1536×1024.

---

# 8. Переход к запуску в Москве

## Решение о рынке

ЦУИМ развивается как масштабируемая городская платформа. Первым крупным рынком выбрана **Москва**, запуск — постепенный. Геоядро проектируется так, чтобы впоследствии добавлять другие города без переделки основы.

## Географическое ядро

**Ключевая точка:** `3fbef9cbdb17f13f45063b923b0ab08ec1b62471`.

- Подключён PostGIS.
- Создан нормализованный справочник географии.
- Иерархия: страна → регион → город → административный округ → район → метро/микрорайон.
- Старые текстовые поля временно сохранены для совместимости.
- Добавлена синхронизация старых и новых геополей.

## Москва: округа, районы и метро

- Добавлены все **12 административных округов Москвы**.
- Добавлены **132 района Москвы**.
- Для части районов импортированы реальные OSM-границы/центры PostGIS.
- Импортированы метро/транспортные узлы с координатами; источники OSM/GTFS + дополнительный набор новых станций.
- Дубли пересадочных узлов нормализованы.
- Проверены новые станции, включая Вавиловскую, ЗИЛ и Новомосковскую.

## Геопоиск и RPC

Созданы функции поиска дочерних объектов, разрешения координаты в район/метро, рекурсивной принадлежности территории, сохранения пользовательского контекста и публичной выдачи с геофильтрами.

Интерфейсный радиус: **1 / 3 / 5 / 10 км**; сервер допускает более широкий диапазон.

## Нягань: историческое скрытие и последующее возвращение в мультигород

- На московском этапе Нягань исключалась из публичного рынка без физического удаления исторических данных.
- Старые пользовательские настройки Нягани мигрировались на Москву.
- 03.09.2026 город возвращён в новый мультигородской селектор вместе с Барнаулом; для Нягани добавлено разрешение ближайших населённых пунктов по геопозиции.

## 02.09.2026 — Москва → округ → район → метро

**Коммит:** `8ab5169ab4f7e6642db518cc50f88706b97128fa`.

- `geo-core.js` расширен московской иерархией.
- Добавлены поиск метро, радиус и определение района/ближайшей станции по геопозиции.
- Контекст хранится локально и для авторизованного пользователя в Supabase.

## 02.09.2026 — локация в поиске

**Коммит:** `66e417e83a91b4367640b1fd4516f0577d9980bf`.

- Desktop получил поле «Локация» в поиске.
- Mobile получил «Настройки поиска» с категорией, компанией и локацией.

## 02.09.2026 — удалён legacy alert

**Коммит:** `6baa87229e1dfc10601eaab01f9fee160382f4a3`.

- Удалено системное окно «Выбор города подключим следующим шагом».
- Кнопка города переведена на геоядро.

## 02.09.2026 — Москва стала автоматической локацией

**Коммит:** `a35be5133c8feadeb155f9bf470ea37eb6c943dd`.

- Убрано состояние «город не выбран».
- Москва устанавливается немедленно при загрузке.
- Старый контекст другого города сбрасывается на Москву.
- Поиск сразу использует формулировку «Что ищете в Москве?».
- Кнопка города стала кнопкой уточнения округа/района/метро/радиуса.

## 03.09.2026 — metro/radius подключены к реальной выдаче

**Статус:** DONE  
**Подсистема:** geo / catalog / beauty / PostGIS

**Коммиты:**

- `515855c` — общий каталог переведён на `marketplace_catalog_products_v4`;
- `1d5dcb6` — Beauty-каталог переведён на `marketplace_beauty_directory_v3`.

Что сделано:

- в RPC передаются `city_id`, `area_id`, `metro_id`, `radius_m`;
- смена района/метро/радиуса перезагружает серверную выдачу;
- карточки могут показывать фактическое расстояние до выбранного метро;
- текущая география отражается в заголовках и выдаче;
- сохранена мягкая совместимость: старые продавцы без координат не исчезают до миграции.

Проверка радиуса:

- внутри транзакции существующий продавец временно размещён у метро Беляево;
- при радиусе 1 км от Беляево получено **14** его предложений;
- при радиусе 1 км от Медведково — **0**;
- расстояние между тестовыми станциями около **28,65 км**;
- транзакция завершена `ROLLBACK`, тестовые координаты не сохранены.

## 03.09.2026 — география новых объявлений

**Коммит:** `33a5d23`.

- `/post-ad.html` подключён к общему `geo-core.js`.
- При создании видна локация Москва/район/метро/радиус.
- В новое объявление сохраняются `marketplace_geo_city_id` и `marketplace_geo_area_id`.
- Точная точка может наследоваться от геопозиции продавца через каталоговый RPC.

## 03.09.2026 — география Beauty-регистрации

**Коммит:** `4b63dca`.

- Старый default «Нягань» удалён; город — Москва.
- Добавлен выбор района/метро.
- Добавлено определение точной точки браузером.
- `marketplace_geo_resolve_point` автоматически определяет район и ближайшее метро.
- Профиль сохраняет `geo_city_id`, `geo_area_id`, а точная точка — PostGIS `geo_point`.

## 03.09.2026 — география компании и зона работы

**Коммит:** `2f88d40`.

- `/account.html` подключён к московскому геоядру.
- Профиль пользователя и компания сохраняют нормализованную Москву/район.
- Для компании можно определить точные координаты.
- Добавлены режимы работы `ONSITE / RADIUS / CITY / REMOTE / HYBRID`.
- Добавлен радиус обслуживания **1 / 3 / 5 / 10 / 20 / 50 км**.
- Локация компании начинает участвовать в выдаче по метро и расстоянию.

### Серверная часть этого этапа

Миграцией `geo_owner_location_writes_v2` созданы:

- `marketplace_update_my_location_v2(...)` — безопасное обновление локации текущей компании;
- `marketplace_beauty_update_my_location(...)` — безопасное обновление локации своего Beauty-профиля/профиля салона.

Обе функции:

- требуют `auth.uid()`;
- проверяют владение сущностью;
- проверяют принадлежность района городу;
- валидируют координаты;
- имеют фиксированный `search_path`;
- **не имеют EXECUTE для `anon`**.

Проверка прав: `anon=false`, `authenticated=true` для обеих новых управляющих функций.

## 03.09.2026 — завершение временного HTTP-импорта

**Статус:** DONE  
**Подсистема:** database / security / geo

- Расширение PostgreSQL `http` использовалось только для импорта московских геоданных.
- Проверено отсутствие пользовательских функций ЦУИМ, зависящих от `extensions.http*`.
- После завершения импорта расширение удалено миграцией `remove_temporary_http_geo_import_extension`.

---

# 9. Безопасность и технический долг

Принципы новой разработки:

- публичные API должны отдавать только данные витрины;
- управляющие операции требуют авторизации и проверки владельца/роли;
- приватные CRM/платёжные сведения не выдаются `anon`;
- для новых `SECURITY DEFINER` фиксируется `search_path` и явно настраивается EXECUTE;
- после DDL выполняется аудит прав и Supabase Advisor;
- геомиграция остаётся совместимой со старыми данными до завершения миграции продавцов.

## 03.09.2026 — аудит Supabase Advisor

После новых geo-write RPC запущен Security Advisor.

**Новые функции:** anon-доступ к двум новым управляющим geo-функциям закрыт и отдельно проверен.

**Обнаруженный существующий технический долг:** Advisor показал значительное количество старых `SECURITY DEFINER` RPC, которым исторически был выдан EXECUTE для `anon` или всех `authenticated`, а также две security-definer view, таблицы с RLS без политик и отключённую leaked-password protection.

## 03.09.2026 — первый системный Supabase Security Hardening

**Статус:** DONE  
**Подсистема:** database / authz / RLS / RPC / Storage

Миграции:

- `security_harden_function_acl_and_internal_views`;
- `security_harden_helper_function_search_paths`;
- `security_revoke_direct_access_to_deny_all_internal_tables`;
- `security_harden_dormant_storage_uploads`;
- `security_minimize_anonymous_table_writes`;
- `security_hide_internal_marketplace_columns`.

Что изменено:

- с внутренних и пользовательских `SECURITY DEFINER` RPC снят лишний `EXECUTE` для `anon`; публичными оставлены только витринные/счётчиковые сценарии, где анонимный вызов является частью продукта;
- `marketplace_open_requests_v2` закрыт от анонимного вызова и теперь проверяет наличие действующего профиля партнёра;
- `set_marketplace_request_media` выведен из браузерного API и оставлен только для доверенного сервисного вызова;
- trigger-функции синхронизации и `handle_new_auth_user` больше не исполняются клиентскими ролями;
- helper-функции ролей/партнёров переведены на фиксированный пустой `search_path` с явными schema-qualified ссылками;
- `inventory_balances` и `payment_totals` переведены в `security_invoker=true`, а прямой `anon SELECT` закрыт;
- для внутренних deny-all таблиц убраны прямые GRANT клиентским ролям; проверка показала **0** прямых прав `anon`/`authenticated` на набор из 22 таких таблиц;
- дополнительно минимизированы старые anonymous table writes и скрыты внутренние marketplace-колонки от прямого Data API.

Проверка:

- `checkout_marketplace_cart_v2`: `anon=false`, `authenticated=true`;
- `courier_open_offers`: `anon=false`, `authenticated=true`;
- `marketplace_admin_reclassify_products`: `anon=false`, `authenticated=true`;
- `set_marketplace_request_media`: `anon=false`, `authenticated=false`, `service_role=true`;
- публичная витрина `marketplace_catalog_products_v4` продолжила работать под `anon` и вернула тестово **14** строк;
- `marketplace_open_requests_v2` без партнёрского контекста вернул **0** строк;
- `inventory_balances` и `payment_totals` под обычным клиентским контекстом не раскрыли внутренние данные.

Ограничение: Supabase Advisor по-прежнему сигнализирует о части намеренно публичных/авторизованных `SECURITY DEFINER` функций и об отключённой leaked-password protection. Защита от утёкших паролей требует отдельной настройки Supabase Auth и в этом проходе не включалась.

---

# 10. Текущий статус продукта

Работают или находятся в рабочем состоянии:

- главная, каталог и таксономия;
- компании, товары и услуги;
- корзина, аккаунт, заказы и отдельная страница заказа;
- публичная карточка предложения `/offer.html`;
- объявления, модерация, сообщения, уведомления;
- рекламная система, YooKassa, ERID;
- Beauty-каталог, мастер/салон, онлайн-запись;
- Beauty Pro календарь и CRM;
- desktop/mobile слайдер и мобильная главная;
- геоядро Москвы, а также мультигородской выбор Москва / Барнаул / Нягань;
- 12 округов, 132 района, метро и PostGIS;
- Москва → округ → район → метро → радиус;
- metro/radius в общем каталоге и Beauty;
- география при создании объявления и в кабинете продавца;
- география Beauty-регистрации;
- география и радиус работы компании;
- maintenance mode с административным управлением;
- мультифото-галерея товаров и публичный слайдер;
- настройки продавца по коммерции, доставке и готовности товара к checkout;
- первый системный проход Supabase Security Hardening.

---

# 11. Очередь следующих задач

1. Добавить географию в **редактирование существующего объявления**.
2. Довести географию **салона Beauty** и сотрудников до единого UX во всех Beauty Pro формах.
3. Дополнить реальные PostGIS-границы районов, которые не сопоставились автоматически.
4. Довести мультигородскую модель: Барнаул и Нягань должны получить полноценные справочники территорий/населённых пунктов и согласованный радиус выдачи.
5. Сделать полноценный режим **«Рядом со мной»** с ранжированием по расстоянию, а не только фильтром метро.
6. Добавить полигоны/зоны доставки и обслуживания там, где радиуса недостаточно.
7. Продолжить второй проход **Supabase Security Hardening**: классифицировать оставшиеся Advisor warnings, убрать лишние definer-RPC там, где достаточно invoker/RLS, и включить leaked-password protection через Auth settings при доступном тарифе.
8. Перед масштабным production-трафиком перенести frontend с GitHub Pages на подходящий CDN/hosting и провести нагрузочный тест.
9. Продолжить финансовый модуль Beauty Pro: стоимость визита, способы оплаты, предоплата, касса и отчётность.

---

# 12. Шаблон новой записи

```text
## ДД.ММ.ГГГГ — Название изменения

Статус: DONE / PARTIAL / ROLLBACK
Подсистема: frontend / database / auth / storage / payments / geo / beauty / ads / admin / mobile

Что сделано:
- ...

Технически:
- таблицы / RPC / страницы / файлы / Edge Functions;
- миграции и совместимость;
- права доступа/RLS.

Проверка:
- сценарий;
- результат;
- rollback тестовых данных при необходимости.

Коммиты:
- `abcdef1` — описание.

Следующий шаг:
- ...
```

---

# 13. Журнал изменений после точки `2f88d40`

## 03.09.2026 — мультигород: Барнаул, Нягань и автоопределение города

**Статус:** DONE  
**Подсистема:** geo / homepage / catalog / database

Что сделано:

- геоселектор расширен с московского сценария до нескольких городов;
- добавлены Москва, Барнаул и Нягань;
- главная фильтрует предложения по выбранному городу;
- при первом посещении браузер может определить город автоматически;
- для Нягани добавлено разрешение ближайших населённых пунктов по координатам.

Миграция: `nyagan_settlement_georesolution`.

Коммиты:

- `02616f2` — Add Barnaul and Nyagan multi-city geo selector;
- `fb1dabc` — Filter marketplace homepage by selected city;
- `08f32c8` — Auto-detect visitor city on first visit;
- `080e69c` — Resolve Nyagan settlements from visitor geolocation.

## 03.09.2026 — maintenance mode для маркетплейса

**Статус:** DONE  
**Подсистема:** admin / frontend / database / operations

Что сделано:

- создан централизованный `maintenance-guard.js`;
- guard подключён к публичным страницам маркетплейса, каталога, Beauty и кабинетов;
- создана `/admin/maintenance.html` для управления режимом техработ;
- добавлен workflow проверки/установки guard на страницы;
- экран техработ приведён к фирменному виду с белым логотипом.

Миграция: `marketplace_maintenance_mode`.

Коммиты: `549b37e`, `be96dee`, `40b92e4`, `8f8b959`, `acaac94`.

## 03.09.2026 — карточка предложения, корзина и оформление заказа

**Статус:** DONE  
**Подсистема:** catalog / checkout / orders / customer account

Что сделано:

- создана единая публичная страница `/offer.html`;
- карточки каталога открывают детальную страницу предложения;
- действия карточек разделены по типу предложения;
- checkout усилен серверной проверкой состава корзины и доступности товара;
- добавлена `/order.html` и переход к деталям заказа из кабинета;
- финализирован клиентский сценарий оформления заказа.

Миграции: `public_marketplace_offer_page`, `marketplace_checkout_v3`.

Коммиты: `d444709`, `97be385`, `ed8c0b6`, `0356ebb`, `3922c01`, `693f120`, `ecfac2b`, `71699f5`, `c1765dc`.

## 03.09.2026 — рекомендации по медиа и мультифото-галерея товара

**Статус:** DONE  
**Подсистема:** seller / catalog / storage / UX

Что сделано:

- зафиксированы правила фото карточки и инфографики в `docs/PRODUCT_MEDIA_GUIDE.md`;
- подсказки встроены в редактор продавца;
- добавлен поиск исполнителя/заявка на создание инфографики;
- продавец может редактировать галерею из нескольких изображений;
- публичная карточка показывает галерею слайдером;
- сохранение и upload получили rollback/валидацию;
- при очистке галереи удаляется legacy cover, чтобы старое изображение не возвращалось.

Миграция: `complete_marketplace_product_image_gallery`.

Ключевые коммиты: `81c62f5`, `2d8b90f`, `a2184a2`, `da86261`, `eb70e0f`, `1603745`, `f91f0da`, `a5e2d50`, `df1b8a0`, `01d30c7`.

## 03.09.2026 — коммерческие настройки продавца и доставка

**Статус:** DONE  
**Подсистема:** seller / checkout / delivery / catalog

Что сделано:

- в кабинет продавца добавлены параметры коммерческой готовности предложения;
- настройки доставки связаны с checkout;
- cart/catalog учитывают признаки доступности доставки и готовности товара к продаже;
- одноразовые installer-файлы после применения удалены из репозитория.

Миграции: `seller_product_checkout_readiness`, `marketplace_cart_products_v2_delivery_flags`.

Коммиты: `dfeec73`, `8602669`, `b5882bf`, `c476bd8`.

## 03.09.2026 — локация конкретного предложения продавца

**Статус:** DONE  
**Подсистема:** seller / geo / catalog

Что сделано:

- в редакторе предложения добавлены элементы управления локацией;
- сохранение предложения связано с текущим geo-context продавца;
- загрузка списка городов получила fallback и стала устойчивее к неполному справочнику;
- временные installer/workflow для разовой установки после выполнения удалены.

Коммиты:

- `d0f0885` — Add seller offer location controls;
- `2d5f645` — Install seller location and geo fallback;
- `a332662` — Add seller location and resilient city loading;
- `b0a97f5`, `9b3fb9e` — cleanup one-off workflow/script.

### Результат ежедневной проверки

- диапазон Git между предыдущей точкой `2f88d40` и новой точкой `9b3fb9e` составляет **49 коммитов**;
- существенные изменения сгруппированы в этапы выше, одноразовые installer/workflow-коммиты отдельно не раздувают журнал;
- фактические Supabase-миграции сверены с историей БД;
- дубликатов ранее учтённых этапов не добавлено;
- новая контрольная точка журнала: `9b3fb9ed82083c72a973c4bd4565feeebded7cc5`.

---

# 14. Журнал изменений после точки `9b3fb9e`

## 04.09.2026 — явные статусы наличия и единые карточки товара

**Статус:** DONE  
**Подсистема:** marketplace / catalog / cart / frontend

Что сделано:

- введены явные состояния наличия `IN_STOCK / LOW_STOCK / ON_ORDER / OUT_OF_STOCK`;
- при наличии явного статуса он берётся из `marketplace_details.stock_status`, при его отсутствии сохраняется совместимый fallback по `quantity`;
- корзина показывает «В наличии», «Осталось N», «Под заказ», «Нет в наличии» и ограничивает количество по доступному остатку;
- создан общий `marketplace-card-ui.js`, подключённый к главной, каталогу и странице продавца;
- карточки получили единый слой отображения и действий без копирования логики между витринами.

Техническое решение: отдельная колонка для статуса не вводилась — состояние хранится в уже типизированном JSON `marketplace_details`, поэтому схема товара не ломалась.

Коммиты:

- `8504dd3` — Add explicit marketplace stock statuses;
- `bf26181` — Add marketplace product card UI enhancer;
- `5bfca09` — Upgrade marketplace product cards.

Проверка: фактический код `cart.html` содержит четыре состояния и fallback, а `marketplace-card-ui.js` подключён в `index.html`, `catalog/catalog.js` и `seller.html`.

## 04.09.2026 — избранное, подтверждённые отзывы и рабочее место продавца

**Статус:** DONE  
**Подсистема:** marketplace / account / reviews / seller / orders / database

Что сделано:

- добавлены клиент избранного и отдельная `/favorites.html`, включая доступ с мобильного интерфейса;
- отзывы о товарах связаны с реальным заказом и получили сценарий подтверждённого отзыва;
- продавец получил отдельные `/seller-orders.html` и `/seller-reviews.html`;
- статусы заказов и допустимые переходы выровнены между покупателем и продавцом;
- продавец может видеть сводку отзывов и отвечать на них;
- добавлены уведомления продавцу о заказах и отзывах.

Миграции:

- `marketplace_verified_product_reviews`;
- `marketplace_favorites_reviews_indexes`;
- `seller_orders_reviews_workspace`;
- `marketplace_order_status_alignment`;
- `seller_order_status_transition_compatibility`;
- `seller_order_ready_cancel_alignment`;
- `seller_order_review_notifications`.

Ключевые коммиты: `923a2b3`, `dd4b1c3`, `cacaf83`, `df2db7a`, `4cf7c3b`, `62df21b`, `a608a20`, `ad50b22`.

Проверка: в фактической БД присутствуют RPC `marketplace_submit_product_review`, `marketplace_my_order_reviews`, `marketplace_partner_reviews`, `marketplace_partner_review_summary` и `marketplace_reply_product_review`.

## 04.09.2026 — уведомления продавца, background push, атомарное сохранение и резервирование остатков

**Статус:** DONE  
**Подсистема:** seller / notifications / push / checkout / inventory / database

Что сделано:

- продавец получил attention-индикаторы и переходы из уведомлений к нужному заказу/отзыву;
- добавлен service worker и background push для продавца;
- сохранение предложения переведено на атомарный RPC `marketplace_partner_save_offer(p_offer jsonb)` вместо набора независимых клиентских записей;
- checkout получил серверное резервирование остатков;
- отмена заказа возвращает резерв и синхронизирует состояние доставки;
- сообщения чата формируют связанные уведомления.

Миграции:

- `seller_background_push_v1`;
- `marketplace_partner_offer_save_v4b`;
- `chat_message_notifications`;
- `checkout_stock_reservation_v4`;
- `marketplace_cancel_restock_and_delivery_sync`.

Ключевые коммиты: `a9c60a2`, `ee27dda`, `1b00158`, `dde80f0`, `725a0dc`, `27d4756`, `7de5497`.

Проверка: атомарный RPC и push-RPC продавца присутствуют в текущей БД; одноразовые installer/workflow после применения удалены отдельными cleanup-коммитами.

## 04.09.2026 — чат, вложения Google Drive и внутренний контур сотрудников

**Статус:** APPLIED / BACKEND  
**Подсистема:** chat / drive / staff / admin / database / security

Фактически применены миграции:

- `chat_drive_attachments`, `chat_drive_attachment_constraints`, `chat_drive_attachment_cleanup_guard`, `chat_drive_attachment_message_index`;
- `google_drive_oauth_connection`, `google_drive_connection_service_table`, `remove_unused_private_drive_connection`;
- `staff_self_registration`, `staff_department_and_position`, `staff_departments_positions_management`, `lock_down_staff_admin_rpcs`, `optimize_staff_structure_indexes`, `staff_admin_edge_service_actions`;
- `preserve_chat_attachments_on_staff_delete`, `private_staff_direct_chats`;
- `chat_message_edit_and_delete_modes`, `harden_chat_message_table_privileges`;
- `chat_push_subscriptions`, `chat_push_hook_secret_helper`, `chat_push_message_trigger`;
- `staff_invitation_links`, `partner_invitation_links`.

Что изменено:

- чат получил модель вложений Google Drive, ограничения, индекс и guard очистки;
- удаление сотрудника не должно уничтожать историю вложений в сообщениях;
- Google Drive connection вынесена в сервисную таблицу, неиспользуемый приватный вариант удалён;
- создана самостоятельная регистрация сотрудников, структура отделов/должностей и административные операции управления ими;
- admin-RPC этого контура отдельно ограничивались миграцией `lock_down_staff_admin_rpcs`;
- появились приватные direct chats сотрудников, режимы редактирования/скрытия/удаления сообщений и усиление прямых table privileges;
- добавлен backend Web Push для чата;
- добавлены токенизированные приглашения сотрудников и партнёров; в БД присутствуют `consume_staff_invite_system` и `consume_partner_invite_system`.

Проверка: миграции присутствуют в production-истории Supabase. На момент этого аудита для части последних backend-only изменений отдельного frontend-коммита в репозитории ещё нет, поэтому им намеренно не приписан SHA.

## 04.09.2026 — ЦУИМ Доставка v2: кабинет курьера, тарифы, выплаты и live tracking

**Статус:** DONE / SECURITY FOLLOW-UP OPEN  
**Подсистема:** delivery / courier / seller / admin / realtime / push / geo / database

Что сделано:

- создан административный раздел доставки и настройки тарифов;
- добавлен кабинет курьера `/courier.html`, onboarding и управление доступностью;
- добавлена страница доставки продавца `/seller-delivery.html` и ссылки на неё из рабочего интерфейса;
- добавлены тарифный quote для продавца, выплаты и история выплат курьера;
- добавлены фоновые push-уведомления курьеру;
- продавец может создавать и отменять courier offer;
- добавлена live-геолокация курьера, передача координат в заказ покупателя и обновление интерфейса доставки;
- одноразовые workflow/installer для внедрения после успешной установки удалены, продуктовые файлы при этом сохранены.

Миграции:

- `delivery_department_v2`;
- `partner_cancel_courier_offer`;
- `delay_fresh_chat_notification_read`;
- `courier_background_push_v2`;
- `delivery_tariffs_and_courier_payouts`;
- `partner_delivery_auto_tariff`;
- `courier_live_location`;
- `grant_authenticated_notifications_access`.

Ключевые коммиты:

- `82ca45a` — Add delivery department dashboard;
- `70359cc` — Add courier cabinet and onboarding;
- `dbb8760` — Add seller delivery management page;
- `120d718`, `6bca38d` — Link/wire delivery management pages;
- `7622da6` — Add delivery tariffs and courier payouts settings;
- `86a3f9d` — Add background push controls for couriers;
- `76cc904` — Add delivery tariff quote helper for sellers;
- `229a95b` — Show courier payout history in courier cabinet;
- `44bbbcc` — Connect delivery tariffs payouts and courier push;
- `e765ddd`, `8f040f3`, `d797a29`, `7d07abb` — live courier location от курьера до заказа клиента;
- `fa5fe6f` — cleanup разового workflow live tracking; текущая продуктовая контрольная точка.

Проверка:

- в production БД присутствуют `courier_delivery_quote`, `courier_my_deliveries_v2`, `courier_my_payouts`, `courier_update_delivery_location`, `partner_delivery_live_location`, `marketplace_my_delivery_live_location`, manager/partner courier RPC;
- Git-диапазон `9b3fb9e..main` на момент проверки составляет **76 коммитов** и затрагивает **32 файла/пути**;
- текущая продуктовая точка репозитория: `fa5fe6f1d5b003eac0aa6206512dce87e0245c40`.

### Security Advisor после новых delivery-миграций

Свежий Security Advisor выявил новый открытый технический долг, который **не считается закрытым**:

- `courier_invites` — RLS выключен;
- `courier_delivery_events` — RLS выключен;
- `courier_delivery_tariffs` — RLS выключен;
- `courier_payout_entries` — RLS выключен;
- `courier_accrue_payout_on_delivery()` остаётся `SECURITY DEFINER` с EXECUTE для `anon`;
- `courier_dispatch_open_offer_push()` остаётся `SECURITY DEFINER` с EXECUTE для `anon`;
- leaked-password protection Supabase Auth всё ещё выключена;
- часть старых/намеренно публичных `SECURITY DEFINER` предупреждений Advisor также сохраняется.

**Решение аудита:** delivery-функциональность зафиксирована как реализованная, но перед production-нагрузкой необходимо отдельно закрыть четыре RLS-ошибки и убрать анонимный EXECUTE с двух внутренних courier-функций.

### Результат ежедневной проверки 04.09.2026

- новые изменения после `9b3fb9e` подтверждены как Git-историей, так и фактическим списком миграций Supabase;
- одноразовые installer/workflow-коммиты не вынесены в отдельные продуктовые этапы;
- backend-only миграции чата, Drive, сотрудников и приглашений записаны без выдуманных SHA;
- дубликаты этапов 03.09.2026 не добавлялись;
- новая контрольная точка журнала: `fa5fe6f1d5b003eac0aa6206512dce87e0245c40`;
- обнаруженные новые security-проблемы доставки явно оставлены открытыми, а не отмечены как решённые.

## 05.09.2026 — мгновенный и проверяемый переключатель maintenance mode

**Статус:** DONE  
**Подсистема:** admin / frontend / operations / database RPC

Что изменено:

- переключатель «Технические работы» в `/admin/maintenance.html` теперь применяет режим сразу при изменении, без отдельного сохранения состояния;
- после вызова `marketplace_admin_set_site_state` страница повторно читает фактическое состояние через `marketplace_admin_site_state` и считает переключение успешным только после подтверждения базы;
- при несовпадении ожидаемого и фактического состояния показывается ошибка, а UI возвращается к состоянию, прочитанному из БД;
- кнопка сохранения теперь отвечает только за заголовок и сообщение экрана техработ;
- добавлены визуальный индикатор фактической доступности, время последнего изменения, ручная перепроверка и тихое обновление состояния при возврате на вкладку;
- во время RPC-операций переключатель и управляющие кнопки блокируются, чтобы исключить конкурирующие повторные действия.

Техническое решение:

- новая миграция не потребовалась: изменение использует существующие административные RPC `marketplace_admin_set_site_state` и `marketplace_admin_site_state`;
- проверка результата вынесена в клиентский read-after-write сценарий вместо оптимистичного доверия успешному ответу записи.

Проверка:

- диапазон после предыдущего журнального коммита `b6bd678` содержит ровно **1** продуктовый коммит;
- изменён только `admin/maintenance.html`: **+30 / −10** строк;
- фактический код содержит `applyMode()`, повторный `readState()` после записи и проверку `verified.maintenance_mode !== desired`;
- дубликат исходного этапа создания maintenance mode не добавлялся — зафиксировано только новое поведение административного переключателя.

Коммит:

- `e3f1aa5` — Make marketplace maintenance toggle immediate and verified.

Новая контрольная точка журнала: `e3f1aa5884103252f4f13069632b7ca109212ec3`.

## 07.09.2026 — POS: идемпотентные продажи, журнал чеков и кассовые операции

**Статус:** APPLIED / BACKEND  
**Подсистема:** POS / cash / MoySklad / analytics / database / authz

Что изменено:

- запись POS-продажи сделана идемпотентной по паре `organization_id + moysklad_sale_id`, поэтому повторная синхронизация обновляет существующую продажу вместо создания дубля;
- добавлен отдельный RPC `get_pos_receipt_history(p_limit, p_search)` с поиском по номеру продажи, оператору, покупателю, способу оплаты, сумме и позициям чека;
- история чеков показывает возвраты, кассу, смену и человекочитаемый источник данных;
- разрешён учёт импортированных продаж МойСклад со статусом `IMPORTED_MS`, включая продажи без сопоставленного локального оператора;
- `pos_dashboard` переведён на реестр `pos_sales` и учитывает как продажи A4PRINT KASSA, так и импорт МойСклад; добавлены отдельные счётчики источников;
- подписи источников/статусов в истории и аналитике нормализованы для интерфейса;
- добавлен backend-контур ручных кассовых операций и расчёт кассы смены с учётом продаж, возвратов, внесений и выплат.

Миграции production Supabase:

- `20260907054958` — `pos_sale_idempotency`;
- `20260907095145` — `pos_receipt_history`;
- `20260907105920` — `allow_imported_moysklad_pos_sales`;
- `20260907110336` — `pos_dashboard_use_receipt_ledger`;
- `20260907111340` — `polish_pos_receipt_history_labels`;
- `20260907112031` — `pos_dashboard_source_labels`;
- `20260907121439` — `pos_cash_operations`.

Важные технические решения:

- `record_pos_sale(...)` требует контекст A4PRINT, роль `ADMIN` или `POS_OPERATOR`, проверяет кассовый счёт и использует `ON CONFLICT` для идемпотентности;
- синхронизация продажи создаёт приходную `cash_transactions` только при отсутствии уже учтённой операции `MOYSKLAD_POS`;
- `get_pos_receipt_history(...)` и `pos_dashboard(...)` — `SECURITY DEFINER`, но внутри проверяют активного пользователя и роль `ADMIN/POS_OPERATOR`; обычный оператор ограничен собственными продажами, администратор может видеть магазин целиком;
- импортированные продажи явно маркируются как «МойСклад»/`IMPORTED_MS`, чтобы не смешивать их с продажами локальной кассы;
- `pos_shift_summary(...)` считает продажи, возвраты, внесения, выплаты и итог `cash_in_register` на одном серверном расчёте.

Проверка:

- все семь новых миграций присутствуют в фактической production-истории Supabase после последней записи журнала;
- в БД подтверждены RPC `record_pos_sale`, `get_pos_receipt_history`, `pos_dashboard`, `pos_shift_summary` и триггер `sync_pos_return_cash_transaction`;
- в `record_pos_sale` подтверждён `ON CONFLICT (organization_id,moysklad_sale_id)` и защита от повторного создания приходной кассовой транзакции;
- в `get_pos_receipt_history` и `pos_dashboard` подтверждены проверки `ADMIN/POS_OPERATOR` и ограничение обычного POS-оператора своим `user_id`;
- новых продуктовых Git-коммитов после `e3f1aa5` не найдено; продуктовая контрольная точка остаётся `e3f1aa5884103252f4f13069632b7ca109212ec3`, поэтому backend-этап не привязан к выдуманному SHA.

Коммиты продукта: новых нет. Коммит этой журнальной записи фиксируется отдельно и не считается новой продуктовой контрольной точкой.

## 07–08.09.2026 — кассовый baseline, возвраты и Realtime рабочего места HUB

**Статус:** APPLIED / BACKEND  
**Подсистема:** POS / cash / returns / realtime / production / payments

Что изменено:

- добавлен явный baseline остатка наличных для POS-кассы, чтобы текущий физический остаток можно было согласовать с историей операций без переписывания старых транзакций;
- исправлена синхронизация возврата в кассу: добавлено требуемое уникальное ограничение/совместимый upsert-контур для `sync_pos_return_cash_transaction`;
- включён Realtime-контур центра управления HUB;
- отдельно выданы необходимые authenticated-доступы для Realtime по production jobs и payments без открытия анонимного доступа;
- добавлен профиль смены POS, чтобы серверная сводка смены могла работать с явным профилем/контекстом кассира.

Миграции:

- `20260907175934` — `add_pos_cash_balance_baseline`;
- `20260907185933` — `fix_pos_return_cash_upsert_constraint`;
- `20260907192555` — `enable_hub_control_center_realtime`;
- `20260908052749` — `grant_authenticated_production_jobs_access`;
- `20260908054334` — `grant_payments_realtime_workspace`;
- `20260908131241` — `add_pos_shift_profile`.

Проверка:

- миграции присутствуют в production-истории Supabase;
- `record_pos_sale`, `pos_shift_summary` и `create_customer_invoice` остаются недоступны `anon`;
- RLS включён на `production_jobs` и `payments`.

Коммиты продукта: новых Git-коммитов после журнального `eb873b5` не найдено.

## 08.09.2026 — второй Security Hardening: courier, support/POS RPC и публичные write-RPC

**Статус:** DONE / AUTH FOLLOW-UP OPEN  
**Подсистема:** security / courier / support / POS / RPC / Data API

Что изменено:

- закрыт ранее отмеченный security follow-up доставки: `courier_invites`, `courier_delivery_events`, `courier_delivery_tariffs` и `courier_payout_entries` переведены под RLS;
- усилен прямой Data API для courier-контура;
- пересмотрены внутренние Support/POS RPC и сняты лишние клиентские права там, где вызов не должен идти напрямую из браузера;
- исправлены оставшиеся исторические `PUBLIC` grants на внутренних функциях;
- публичные write-RPC получили отдельную защиту от злоупотреблений.

Миграции:

- `20260908093750` — `harden_courier_data_api`;
- `20260908095107` — `harden_internal_support_pos_rpcs`;
- `20260908095124` — `fix_internal_function_public_grants`;
- `20260908095406` — `public_write_rpc_abuse_protection`.

Проверка:

- фактически подтверждено `relrowsecurity=true` для `courier_invites`, `courier_delivery_events`, `courier_delivery_tariffs` и `courier_payout_entries`;
- предыдущая запись 04.09 о выключенном RLS для этих четырёх таблиц считается закрытым историческим follow-up, а не текущим состоянием;
- leaked-password protection Supabase Auth остаётся отдельной настройкой и этим набором DB-миграций не закрывается.

Коммиты продукта: новых нет.

## 08.09.2026 — подготовка продавцов Москвы к запуску

**Статус:** APPLIED / BACKEND  
**Подсистема:** marketplace / sellers / Moscow / admin / geo / readiness

Что изменено:

- введён серверный readiness-gate для московских продавцов;
- администратор получил сводку готовности партнёров и детальную проверку конкретного продавца;
- продавцу добавлен self-service RPC обновления московского публичного профиля, адреса/координат, режима обслуживания и радиуса;
- административное обновление профиля вынесено в отдельный guard-защищённый RPC.

Миграции:

- `20260908145917` — `moscow_seller_readiness_gate`;
- `20260908150227` — `add_moscow_seller_admin_readiness_tools`;
- `20260908151042` — `add_moscow_partner_self_service_profile`.

Важное техническое решение:

- readiness рассчитывается на сервере через `marketplace_partner_readiness(...)`, а admin-выдача использует `marketplace_admin_guard()`; это не клиентская эвристика и не доверие полям формы.

Проверка:

- в production подтверждены две перегрузки `marketplace_admin_partner_readiness(...)`;
- admin RPC являются `SECURITY DEFINER` и внутри явно проверяют `marketplace_admin_guard()`.

Коммиты продукта: новых нет.

## 08.09.2026 — единая цепочка производство → заказ → платежи → документы

**Статус:** APPLIED / BACKEND  
**Подсистема:** production / orders / payments / customers / documents / RLS

Что изменено:

- статус заказа синхронизируется с производственным заданием через серверный trigger-контур;
- trigger отдельно усилен, чтобы не превращать техническую функцию в клиентский RPC;
- платежи связаны с документами клиента;
- для документов добавлены HUB staff policies;
- нормализованы defaults платежей и отдельно исправлен default типа платежа;
- при создании клиентского счёта автоматически выпускается документ `INVOICE` с уникальным номером и snapshot реквизитов клиента;
- `order_status_history` получил authenticated-доступ, необходимый рабочему HUB-контексту, при сохранённом RLS.

Миграции:

- `20260908151405` — `sync_order_status_from_production`;
- `20260908151429` — `production_job_order_status_trigger`;
- `20260908151454` — `harden_production_order_status_trigger`;
- `20260908152228` — `link_payments_to_customer_documents`;
- `20260908152320` — `documents_hub_staff_policies`;
- `20260908152405` — `normalize_hub_payments_defaults`;
- `20260908153209` — `fix_hub_payment_type_default`;
- `20260908153445` — `issue_customer_invoice_on_creation`;
- `20260908164133` — `grant_order_status_history_authenticated`.

Проверка:

- `create_customer_invoice(...)` в production требует роль `ADMIN` или `MANAGER`, проверяет положительную сумму и существование клиента;
- номер счёта формируется серверно, а реквизиты фиксируются snapshot-объектом в документе;
- RLS включён на `documents`, `customer_documents`, `payments`, `production_jobs` и `order_status_history`.

Коммиты продукта: новых нет.

## 08.09.2026 — оборудование, расходники и сервисная история

**Статус:** APPLIED / BACKEND  
**Подсистема:** equipment / inventory / consumables / service / database / security

Что изменено:

- добавлен контур учёта оборудования и связанных расходных материалов;
- складской остаток расходников защищён от ухода в недопустимое состояние;
- генератор инвентарных номеров усилен как внутренняя функция;
- сервисный журнал синхронизирует даты обслуживания оборудования.

Миграции:

- `20260908165124` — `equipment_inventory_and_consumables`;
- `20260908165154` — `protect_consumable_stock_balance`;
- `20260908165901` — `harden_equipment_inventory_number_function`;
- `20260908171610` — `equipment_service_log_sync`.

Проверка:

- в production присутствуют функции `next_equipment_inventory_number`, `check_equipment_consumable_stock`, `sync_equipment_service_dates` и `touch_equipment_updated_at`;
- все четыре миграции присутствуют в фактической истории проекта.

Коммиты продукта: новых нет.

### Результат ежедневной проверки 08.09.2026

- после предыдущего журнального коммита `eb873b5` новых продуктовых Git-коммитов не найдено;
- добавлены только фактически присутствующие production-миграции после уже учтённой `pos_cash_operations`;
- ранее открытый courier RLS follow-up отмечен как закрытый только после проверки фактического `relrowsecurity=true` на четырёх таблицах;
- backend-only этапам не присвоены выдуманные продуктовые SHA;
- продуктовая контрольная точка остаётся `e3f1aa5884103252f4f13069632b7ca109212ec3`.

---

# Полный технический след

Этот журнал показывает **процесс создания продукта**. Полная механическая история каждой правки хранится в Git начиная с `cffe249f4e0e58a371cbf79a5a140edfc0da1937`.

При расхождении журнал уточняется по Git и фактическому состоянию Supabase. История Git ради журнала не переписывается.